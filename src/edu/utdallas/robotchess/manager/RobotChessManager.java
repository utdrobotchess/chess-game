package edu.utdallas.robotchess.manager;

public class RobotChessManager extends ChessManager
{
    //Copy over stuff from ChessManager, or make an implementable class.
    //Actually, we could just extend ChessManager. Eventually we should clean
    //up the Manager classes.
    @Override
    public void handleSquareClick(int index)
    {
        super.handleSquareClick(index);
    }
}
