TODO

1) Add Panel that displays robot ordered addresses and shows if they are
    currently connected

2) Add feature that kills the RemoteController thread

3) Add feature that kills ChessbotCommunicator thread

4) Add option to assign a robot another ID

5) Add option to assign a remote controller to available robots

6) Add feature that does not let Remote Controller send to the same robot that
   is taking commands in demo mode

7) Add feature in remote controller that does not sending the same command in
   a row

8) Add feature to display whether XBee found on comport
